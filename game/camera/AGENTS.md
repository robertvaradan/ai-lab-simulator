# Isometric camera

This folder owns the gameplay isometric camera.

- Follow `../../docs/presentation/isometric-camera.md`.
- The camera must use orthogonal projection.
- The camera must not rotate.
- Pan and zoom must use smoothing.
- Automated capture must disable camera input and snap to the authored pose.
